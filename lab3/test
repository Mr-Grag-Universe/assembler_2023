flag - поднят если мы встретили начало плохого слова
space_f - поднимаем, если встретили пробел

; если ' ' то выставляем флаги, копируем и идём дальше
if (s[i] == ' ') {
	flag = 0
	s[j] = s[i]
	++j
	space_f = 1
	continue
}

; если мы идём по плохому слову, пропускаем его
if (flag == 1)
	continue

; если это первый символ слова и он не chr то слово плохое - ставим флаги
if (s[i] != chr && space_f)
	flag = 1
	space_f = 0
	continue

; если это не первый символ слова и оно хорошее то копируем его
s[j] = s[i]
++j



++++++++++++++++++++

collaps
handle
collaps
handle
strip_right()
if (delim_new) {
	std::cout << 
}
